{% extends "base.jinja" %}
{% block body %}
    <div>
        <div class="titleDiv">
            Courses
        </div>
        <div class="centerDiv tableBorderColor">
            <table class="table">
            <thead>
              <tr>
                <th>Course</th>
                <th>Grade</th>
              </tr>
            </thead>
            <tbody>
                {% for s in section_list %}
                    {% if student %}
                    <tr class="collapsible-header">
                    {% else %}
                    <tr>
                    {% endif %}
                        <td>
                            {% if not student %}
                            <a href="/course/{{s['COURSE_ID']}}/{{s['ID']}}">
                            {% endif %}
                            Course ID: {{ s['COURSE_NAME'] }}
                            </br>
                            Section ID: {{ s['ID'] }}
                            {% if not student %}
                            </a>
                            {% endif %}
                        </td>
                        <td>
                            {% if student %}
                            {{ s['grade']['VALUE'] }}
                            {% else %}
                            {{ s['MAX_STUDENTS'] }}/{{ s['MAX_STUDENTS'] }}
                            <br>
                            graded
                            {% endif %}
                        </td>
                    </tr>
                    {% if student %}
                    <tr class="collapsible-content">
                        <td colspan="2">
                            <p><strong>Comments</strong></p>
                            {% for c in s["comments"] %}
                            <p>
                                {{ c["FIRSTNAME"] }} {{ c["LASTNAME"] }} ({{ c["ROLE"] }}) - Posted {{ c["CREATED_TIME"] }}
                                <br>
                                {{ c["comment"] }}
                            </p>
                            {% endfor %}
                            <input type="text" style="width: 80%" placeholder="Write a comment">
                            <button style="float: right">Submit</button>
                        </td>
                    </tr>
                    {% endif %}
            {% endfor %}
            </tbody>
          </table>
        </div>
    </div>
{% endblock %}